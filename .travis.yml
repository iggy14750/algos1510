before_install:
  - sudo apt-get update
  - sudo apt-get install texlive-latex-extra
script:
  - mkdir _build
  - pdflatex -output_directory=_build assignment5/a5.tex
after_success:
  - curl --ftp-create-dirs -T _build/a5.pdf -u $PITT_USER:$PITT_PW sftp://unixs.cis.pitt.edu/$PITT_PATH